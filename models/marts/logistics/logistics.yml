
version: 1

models:
  - name: days_diff
    description: "Table with days difference information"

    columns:
      - name: order_status
        data_tests:
          - accepted_values:
              arguments:
                values: ['canceled', 'delivered', 'invoiced', 'processing', 'shipped', 'unavailable', 'created', 'approved']
